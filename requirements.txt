Django>=5.2.0,<5.3
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
django-filter==24.3
django-environ==0.11.2
psycopg[binary]>=3.2.10,<3.4
hdbcli==2.23.27
openpyxl>=3.1.5,<3.2
gunicorn==23.0.0
firebase-admin>=6.5.0,<7
gspread>=6.1.0,<7
google-auth>=2.35.0,<3
# Optional: only used when REDIS_URL is set (shared cross-worker cache backend).
# Harmless to install when unset — the LocMemCache default never imports it.
redis>=5.0.0,<6
# Optional: only used when ANTHROPIC_API_KEY is set. The AI chatbot works fully
# offline on its built-in rules engine without this; installing it + setting the
# key upgrades the bot to Claude tool-use. Uncomment to enable.
# anthropic>=0.40.0,<1
